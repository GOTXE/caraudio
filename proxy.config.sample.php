<?php
/**
 * Copia este archivo como `proxy.config.php` y ajusta el valor.
 */

// Base URL del Navidrome publicado (https://... sin /rest al final)
$NAVIDROME_BASE_URL = 'https://navimusic.dimoti.myds.me';

// Solo si usas un destino con certificado no válido (no recomendado).
$ALLOW_INSECURE_TLS = false;

